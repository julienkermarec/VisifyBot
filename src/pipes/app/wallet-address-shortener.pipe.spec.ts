import { WalletAddressShortenerPipe } from './wallet-address-shortener.pipe';

describe('WalletAddressShortenerPipe', () => {
  it('create an instance', () => {
    const pipe = new WalletAddressShortenerPipe();
    expect(pipe).toBeTruthy();
  });
});
